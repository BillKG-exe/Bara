import React from 'react'

function SpaceDivider({ height, width, margin, padding}) {
    return (
        <div style={{
            display: "block",
            height: height,
            width: width,
            padding: padding,
            margin: margin,
            /* backgroundColor: "aqua" */
        }}>
            
        </div>
    )
}

export default SpaceDivider
